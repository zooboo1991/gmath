@AGENTS.md

# CLAUDE.md — gmath.mn

## Төслийн тухай

Олимпиадын математикийн сургалтын платформ (gmath.mn).
Хэрэглэгчид: эцэг эхчүүд болон 4–12-р ангийн сурагчид, багш нар.

Гол урсгалууд:

1. **Сургалт** — бүртгүүлэх → төлбөр (QPay эсвэл дансаар) → Zoom хичээл /
   бичлэг → сертификат
2. **Түвшин тогтоох** — гурван төрөл: Энгийн, Сонгон (тест, AI зөвлөмж),
   Олимпиад (бодолтоо зургаар илгээж багш засаад дүгнэлт бичнэ)
3. **Контент** — нийтлэл, багшийн танилцуулга, сургалтад холбогдсон нийтлэл

## Tech stack

- **Next.js 16.2.11, App Router** (`src/app/`), React 19, TypeScript strict
- **Backend нь тусдаа биш** — ижил Next.js апп доторх route handler-ууд
  (`src/app/api/**/route.ts`). Тусдаа сервер, тусдаа репо байхгүй
- **Өгөгдлийн сан: Supabase Postgres**, `@supabase/supabase-js`-ээр PostgREST
  дуудна (`src/lib/supabase.ts` → `src/lib/db.ts`). ORM хэрэглэдэггүй.
  Схемийг `supabase/schema.sql`-д нэмээд **хэрэглэгч гараар** Supabase SQL
  Editor дээр ажиллуулна — миграцийн хэрэгсэл байхгүй
- **Локал болон продакшн НЭГ ижил өгөгдлийн сан хуваалцдаг.** Тестийн өгөгдөл
  үүсгэвэл заавал устгах
- **Auth: өөрийн бичсэн** — Supabase Auth, NextAuth аль нь ч биш.
  `sessions` хүснэгт + HMAC-аар гарын үсэг зурсан cookie (`src/lib/session.ts`),
  нууц үг scrypt + хэрэглэгч тус бүрийн salt (`src/lib/password.ts`).
  Нэг аккаунт дээд тал нь 2 төхөөрөмж (`MAX_SESSIONS_PER_USER`)
- **Стиль: Tailwind v4** — `src/app/globals.css` дэх `@theme` дотор өнгө,
  сүүдэр, breakpoint. Тусдаа `tailwind.config` файл байхгүй
- **Төлбөр: QPay v2** (`src/lib/qpay/`), нэхэмжлэх үүсгэх + callback webhook
- **Видео:** амьд хичээл Zoom (Server-to-Server OAuth, ирц бүртгэх webhook);
  бичлэг **Bunny Stream** (гарын үсэгтэй, хугацаатай embed) эсвэл гадаад
  Drive/YouTube холбоос. Танилцуулга бичлэг YouTube
- **AI:** Anthropic SDK. Монгол хэлэнд Sonnet, хурдан ажилд Haiku
  (`src/lib/ai/`). DeepSeek нөөц провайдер байгаа
- **SMS:** Skytel (OTP, мэдэгдэл). **Push:** web-push (VAPID)
- **Deploy: Vercel** (hnd1). `main` салбарт push хийхэд автоматаар deploy.
  Cron: хичээлийн сануулга, товлосон нийтлэл (`vercel.json`)

## Ерөнхий дүрэм

- Надтай **монголоор** харилцана, кодын comment **англиар**
- Хэрэглэгчид харагдах бүх текст **монголоор** — кирилл үсгийн алдаагүй,
  албан ёсны найруулгатай
- Байгаа pattern-ийг дага — шинэ библиотек, шинэ арга барил нэмэхийн өмнө асуу
- Энгийн байлга: шаардлагагүй abstraction, асуугаагүй feature бүү нэм
- **Том зүйл дээр эхлээд зөвшөөрөл ав**: шинэ боломж, өгөгдлийн сангийн
  схемийн өөрчлөлт, төлбөр/эрхийн логик, олон файл хамарсан цэгцлэл,
  гуравдагч үйлчилгээ нэмэх. Эхлээд товч төлөвлөгөө хэлж, "хийе" гэсний
  дараа хэрэгжүүл
- **Жижиг зүйлийг шууд хий**: текст, өнгө, зай, нэг файлын алдаа засах,
  ойлгомжгүй мессеж сайжруулах. Тайлбар нь ажлаасаа урт байх ёсгүй

## Чухал хэсгүүд (онцгой болгоомжтой)

- **Төлбөрийн логик (QPay):** өөрчлөхийн өмнө заавал асуу. Callback нь
  idempotent — нөхцөлт UPDATE-ээр (`.eq("status", <хүлээгдэж буй>)`) нэг
  л удаа бичигдэнэ. `sender_invoice_no` дахин ашиглагдахгүй. Callback заавал
  `200` + яг `SUCCESS` буцаана
- **Auth/эрх:** сурагч зөвхөн ӨӨРИЙН сургалт, бичлэг, Zoom холбоос,
  сертификатад хандана
- **Бичлэгийн хандалт:** URL мэдээд ч нэвтрэхгүй байх ёстой. Одоогийн
  хэрэгжүүлэлт: `/api/lessons/recording` нь (1) нэвтэрсэн эсэх,
  (2) тухайн сургалтад **status='active'** бүртгэлтэй эсэхийг шалгана —
  өөр бол 404 (403 биш: ямар бичлэг байгааг ч мэдэгдэхгүй). Дараа нь
  сервер талд гарын үсэг зурсан, 3 цагийн хугацаатай холбоос үүсгэнэ.
  Холбоос хуудсанд хэзээ ч бичигддэггүй
- **Админы эрх:** хоёр түвшин — `full` (бүх зүйл) ба `viewer` (зөвхөн
  харах). `src/lib/adminSections.ts` (client-safe) + `adminAccess.ts` (server)
- **Хүүхдийн мэдээлэл:** сурагчдын хувийн мэдээллийг лог, алдааны мессежид
  бүү оруул

## Testing

- **Автомат тестийн framework байхгүй.** Vitest, Jest, Playwright аль нь ч
  суугаагүй, `*.test.ts` файл байхгүй
- Батлах арга нь: `npx tsc --noEmit` → `npm run lint` → `npm run build` →
  дараа нь **бодит өгөгдөл дээр шалгах** (browser эсвэл curl-ээр). Аль болох
  нэг таамаг — нэг шалгаж болох баримт
- Тестийн өгөгдөл үүсгэвэл **өөрийн үүсгэсэн мөрийг л** устга (id-гаар нь).
  Хүснэгтийг бүхэлд нь устгах, `.neq("id","")` маягийн бөөнөөр устгах хориотой —
  локал, продакшн нэг сан хуваалцдаг
- Хэрэглэгчийн жинхэнэ өгөгдөл рүү гараар PUT/POST туршилт явуулахгүй.
  Заавал шаардлагатай бол тусдаа тестийн мөр үүсгэ
- Алдаа олбол засахгүй, `BUGS.md`-д бүртгэ (файл одоогоор байхгүй, эхний
  алдаанд үүснэ)

## Commands

```bash
npm run dev      # локал сервер (http://localhost:3000)
npm run build    # продакшны build — commit хийхийн өмнө заавал
npm run lint     # eslint
npx tsc --noEmit # төрлийн шалгалт (тусдаа script байхгүй)
```

`npm test` байхгүй.

## Ажлын урсгал

Хэрэгжүүлэх → `tsc` → `lint` → бодит өгөгдөл дээр шалгах → `build` →
staged diff дээр нууц утга хайх → commit → `git push origin main` →
монголоор тайлагнах.

Схем өөрчлөгдвөл: `supabase/schema.sql`-д нэмээд SQL-ийг хэрэглэгчид өгнө —
Supabase дээр **тэр өөрөө** ажиллуулна. Vercel-ийн env хувьсагчийг мөн адил
хэрэглэгч тохируулж, **шинээр deploy хийж** байж хүчинтэй болно.
