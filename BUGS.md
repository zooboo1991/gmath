# BUGS.md

Автомат тест бичих явцад олдсон алдаанууд. **Энд бүртгэсэн алдааг зассангүй** —
QA-гийн ажил нь олж баримтжуулах, засах эсэхийг шийдэх нь эзний эрх.

Алдаа бүрийг тест давтдаг. Тэдгээр тестийг `it.fails(...)`-ээр тэмдэглэсэн:
**зөв** зан төлөвийг батлах бичигдсэн тул алдаа засагдмагц ногоон болно, харин
хэн нэгэн тестийг нь устгавал шууд анзаарагдана.

---

## #1 — Амжилттай нэвтрэлт ч түгжээний тоололд ордог — ✅ ЗАССАН

**Хаана:** [src/app/api/account/login/route.ts:18](src/app/api/account/login/route.ts:18)
(мөн [src/lib/rateLimit.ts:25](src/lib/rateLimit.ts:25))

**Юу болж байна:** `checkRateLimit("login:<phone>", 8, 5*60)` нь нууц үг
шалгагдахаас **өмнө** дуудагдаж, тоолуурыг нэмэгдүүлдэг. Тоолуур зөвхөн
амжилтгүй оролдлогыг бус, **дуудлага бүрийг** тоолно.

**Хэрхэн давтах:**

```
5 минутын дотор ЗӨВ нууц үгээр 8 удаа нэвтэр → бүгд 200
9 дэх удаа ЗӨВ нууц үгээр нэвтэр      → 429 "Оролдлого хэт олон удаа..."
```

Тест: [tests/api/auth/login.test.ts](tests/api/auth/login.test.ts) →
`does not lock out a user whose password is correct every time`

**Эрсдэл:** дунд зэрэг. Нэг аккаунтыг 2 төхөөрөмж хуваалцахаар зохиосон
(`MAX_SESSIONS_PER_USER = 2`) — эцэг эх, хүүхэд хоёр ээлжлэн нэвтрэх, эсвэл
клиент дахин оролдох үед хэрэглэгч өөрийгөө 5 минут түгжинэ. Аюулгүй байдлын
нүх биш, харин жинхэнэ хэрэглэгчид тулгарах саад.

**Засвар:** `rateLimit.ts`-д уншиж шалгах, тоолох хоёрыг салгав —
`peekRateLimit` (тоолуурыг хөндөхгүй), `recordFailedAttempt`, `clearRateLimit`.
Нэвтрэх route одоо эхлээд peek хийж (түгжигдсэн дугаар нууц үгийн удаан
шалгалт руу ч хүрэхгүй), нууц үг буруу үед л тоолуурыг нэмэгдүүлж, зөв үед
тоолуурыг цэвэрлэнэ.

`checkRateLimit`-ийг хэвээр үлдээв: чат, хуудасны үзэлт зэрэг дуудлага бүр нь
хязгаарлах ёстой зүйл байдаг 13 газар түүнийг ашигладаг.

Тест хоёр талтай: амжилттай нэвтрэлт тоологдохгүй, мөн амжилттай нэвтрэлт
өмнөх алдаануудыг цэвэрлэнэ ([tests/api/auth/login.test.ts](tests/api/auth/login.test.ts)).

---

## #2 — Нууц үг сэргээхэд өмнөх сессионууд хүчинтэй хэвээр үлддэг — ✅ ЗАССАН

**Хаана:** [src/app/api/account/reset-password/route.ts:52](src/app/api/account/reset-password/route.ts:52)
(мөн [src/lib/session.ts:8-12](src/lib/session.ts:8) — энэ дутагдлыг кодын
тайлбарт өөрт нь хүлээн зөвшөөрсөн байгаа)

**Юу болж байна:** `updateUserPassword` нь зөвхөн `users` мөрийн hash-ийг
солино. `sessions` хүснэгтийн мөрүүд хэвээрээ үлдэх тул хуучин cookie-тэй
төхөөрөмж нэвтэрсэн хэвээр байна.

**Хэрхэн давтах:**

```
1. A төхөөрөмжөөс нэвтэр                        → сессион үүснэ
2. OTP-оор нууц үгээ сэргээ (өөр төхөөрөмжөөс)  → 200
3. A төхөөрөмжөөс GET /api/account/me           → хэрэглэгч БУЦСААР байна
```

Тест: [tests/api/auth/reset-password.test.ts](tests/api/auth/reset-password.test.ts) →
`signs other devices out when the password is changed`

**Эрсдэл:** өндөр. Нууц үг сэргээх үйлдлийг хүн яг л "хэн нэгэн миний
аккаунтад орсон байна" гэж сэжиглэсэн үедээ хийдэг. Одоогийн байдлаар тэр
үйлдэл халдагчийг гаргадаггүй — халдагчийн сессион 30 хоног хүчинтэй хэвээр
(`maxAge: 60*60*24*30`, [session.ts:100](src/lib/session.ts:100)).

**Засвар:** `updateUserPassword` ([src/lib/db.ts:585](src/lib/db.ts:585)) нь
нууц үгийг сольсныхоо дараа тухайн `user_id`-ийн бүх `sessions` мөрийг устгадаг
болов. Сессион устгах нь дуудагчийн ажил биш, энэ үйлдлийн салшгүй хэсэг —
нууц үгээ сольсон хүн бүр хуучин төхөөрөмжүүдээ гаргана. Сэргээлт хийсэн
төхөөрөмж нь дараа нь `setSessionUser`-ээр шинэ сессион авдаг тул нэвтэрсэн
хэвээр үлдэнэ.

Тест хоёр талыг нь барина: бусад төхөөрөмж гарсан эсэх, сэргээлт хийсэн
төхөөрөмж үлдсэн эсэх ([tests/api/auth/reset-password.test.ts](tests/api/auth/reset-password.test.ts)).

---

## #4 — Дүүрсэн ангид сурагч ӨӨРИЙН эхлүүлсэн төлбөрөө үргэлжлүүлж чадахгүй — ✅ ЗАССАН

**Хаана:** [src/app/api/enroll/route.ts:57-65](src/app/api/enroll/route.ts:57)

**Юу болж байна:** суудлын шалгалт нь сурагчийн өөрийнх нь бүртгэлийг хайхаас
([route.ts:108](src/app/api/enroll/route.ts:108)) **өмнө** ажиллана.
`countRegistrationsForProgram` нь `pending` мөрийг ч тоолдог тул сурагчийн
өөрийнх нь эзэлсэн суудал ангийг "дүүрсэн" болгож, дараа нь тэр сурагч рүү
409 буцаана.

**Хэрхэн давтах:**

```
capacity = 1 сургалт
1. Сурагч POST /api/enroll {payMethod:"qpay"} → 200, pending бүртгэл + QPay QR
2. Тэр сурагч QR цонхоо хаагаад дахин нээнэ (ижил хүсэлт)
   → 409 "Энэ ангийн бүртгэл дүүрсэн байна."
```

Тест: [tests/api/payment/enroll.test.ts](tests/api/payment/enroll.test.ts) →
`lets a student who already holds a seat resume their own payment`

**Эрсдэл:** дунд зэрэг. Сурагч нэхэмжлэхээ дахин нээж чадахгүй, суудал нь
эзлэгдсэн хэвээр үлдэнэ. QPay-ийн `sender_invoice_no` дахин ашиглагдахгүй тул
шинэ нэхэмжлэх ч үүсгэж болохгүй — цуцлаад дахин эхлэхээс өөр гарц алга, тэр
нь бүртгэлийн id-г мэдэж байх шаардлагатай. Дүүрэх дөхсөн анги дээр л илэрнэ.

**Засвар:** `countRegistrationsForProgram`-д `excludeUserId` сонголт нэмж,
бүртгэлийн route түүгээр дамжуулдаг болов. "Анги хэр дүүрсэн бэ" гэдэг нь
"ЭНЭ сурагч үргэлжлүүлж болох уу" гэсэн өөр асуулт — сурагчийн өөрийнх нь
эзэлсэн суудал өөрийнх нь эсрэг тоологдох ёсгүй.

Анхаарах зүйл (эхний оролдлого дээр алдсан): `user_id.neq.X` дангаараа
`user_id IS NULL` мөрүүдийг мөн хаядаг — SQL-д `NULL <> 'x'` нь үнэн биш,
NULL. Тэдгээр нь админ утасны дугаараар нэмсэн, аккаунтгүй сурагчдын бүртгэл
бөгөөд суудал эзэлдэг. Тиймээс `or(user_id.is.null, user_id.neq.X)` болгов.
Байгаа тестүүд үүнийг шууд барьсан.

---

## #5 — Зөвхөн нийтлэлийн холбоос солих PUT нь "сургалт олдсонгүй" гэнэ — ✅ ЗАССАН

**Хаана:** [src/lib/db.ts:733-755](src/lib/db.ts:733) (`updateCourse`) +
[src/app/api/admin/courses/[id]/route.ts:95-106](src/app/api/admin/courses/%5Bid%5D/route.ts:95)

**Юу болж байна:** `updateCourse` нь илгээгдсэн талбаруудаас patch бүтээдэг.
Хэрэв хүсэлтэд сургалтын нэг ч талбар байхгүй бол patch хоосон болж,
PostgREST ямар ч мөр шинэчлэхгүй, `maybeSingle()` `undefined` буцаана →
route нь **404 "Сургалт олдсонгүй"** гэж хариулна. Хамгийн чухал нь
`setProgramArticles` тэр 404-ийн ӨМНӨ биш ХОЙНО байдаг тул нийтлэлийн
холбоос **огт хадгалагдахгүй**.

**Хэрхэн давтах:**

```
PUT /api/admin/courses/<байгаа сургалтын id>
{ "articleIds": ["<байгаа нийтлэлийн id>"] }
→ 404 "Сургалт олдсонгүй", холбоос хадгалагдаагүй
```

Тест: [tests/api/content/courses.test.ts](tests/api/content/courses.test.ts) →
`saves a list sent on its own, without other course fields`

**Эрсдэл:** бага. Одоогийн админ панель `articleIds`-ыг маягтын бусад
талбаруудтай хамт нэг цул объектоор илгээдэг
([CourseObjectPage.tsx:116](src/components/admin/CourseObjectPage.tsx:116)) тул
UI-аас илрэхгүй. Гэхдээ хэсэгчилсэн хадгалалт нэмэх юм уу, өөр клиент бичих
үед чимээгүй унана — мөн "олдсонгүй" гэсэн мессеж нь шалтгааныг нь буруу
заана.

**Засвар:** `updateCourse` нь patch хоосон үед мөрийг шинэчлэхийг оролдохоо
болиод одоо байгаа сургалтыг эргүүлж уншина. "Өөрчлөх зүйлгүй" гэдэг нь
"ийм сургалт байхгүй" гэсэн үг биш.

Хоёр дахь тест нөгөө талыг барина: үнэхээр байхгүй сургалтын id одоо ч 404
буцаадаг байх ёстой — хоосон patch бүх id-г 200 болгож хувиргах ёсгүй.

---

## #3 — Хоосон/эвдэрсэн JSON body 500 буцаана — ✅ ЗАССАН

**Хаана:** [src/app/api/account/login/route.ts:9](src/app/api/account/login/route.ts:9).
Ижил хэв маяг: [register/route.ts:21](src/app/api/account/register/route.ts:21),
[reset-password/route.ts:13](src/app/api/account/reset-password/route.ts:13),
[otp/send/route.ts:8](src/app/api/account/otp/send/route.ts:8),
[otp/verify/route.ts:8](src/app/api/account/otp/verify/route.ts:8),
[enroll/route.ts:25](src/app/api/enroll/route.ts:25),
[lessons/join/route.ts:21](src/app/api/lessons/join/route.ts:21)

**Юу болж байна:** `await request.json()` барьцгүй дуудагдсан тул body хоосон
эсвэл JSON биш үед `SyntaxError` шидэгдэж, 500 буцна. Зарим route энэ асуудлыг
аль хэдийн шийдсэн (`request.json().catch(() => ({}))` —
[lessons/recording/route.ts:21](src/app/api/lessons/recording/route.ts:21)).

Мөн ижил төрлийн асуудал `formData()`-д:
[admin/upload/route.ts:12](src/app/api/admin/upload/route.ts:12),
[admin/problems/upload/route.ts](src/app/api/admin/problems/upload/route.ts) —
multipart биш body ирвэл `TypeError` шидэгдэнэ.

**Хэрхэн давтах:**

```
POST /api/account/login  (body огт байхгүй, Content-Type: application/json)
→ 500, серверийн лог дээр "SyntaxError: Unexpected end of JSON input"

POST /api/admin/upload   (JSON body, админаар нэвтэрсэн)
→ 500, "TypeError: Content-Type was not one of multipart/form-data..."
```

Тестүүд: [tests/api/auth/login.test.ts](tests/api/auth/login.test.ts) →
`answers 400 rather than 500 for a body that is not JSON`,
[tests/api/authz/admin.test.ts](tests/api/authz/admin.test.ts) →
`answer 400 rather than 500 for a body that is not multipart`

**Эрсдэл:** бага. Сессион олгогдохгүй, өгөгдөл алдагдахгүй. Гэхдээ (1) буруу
оролтыг 400-аар хариулдаг бусад бүх тохиолдлоос зөрж байна, (2) лог руу
шаардлагагүй stack trace цутгана, (3) скайнер энэ 500-г "эмзэг цэг" гэж
тэмдэглэдэг.

**Засвар:** долоон route дээр `request.json().catch(() => ({}))`, хоёр
байршуулах route дээр `request.formData().catch(() => null)`. Уншигдахгүй
биетийг хоосон гэж үзээд, тухайн route-ийн өөрийнх нь шалгалт 400-аа
хэлдэг болов — `lessons/recording` route аль хэдийн ийм хэв маягтай байсан,
түүнийг дагав. Алдааны мессежийг route бүр өөрөө эзэмшсэн хэвээр.

---

## #6 — Хүлээгдэж буй бүртгэлийн мэдэгдлийн холбоос хоосон газар аваачна

**Хаана:** [src/components/profile/ProfileClient.tsx:72](src/components/profile/ProfileClient.tsx:72)
(мэдэгдлийг үүсгэдэг тал: [src/lib/db.ts](src/lib/db.ts) — `notifyBankTransferPending`)

**Юу болж байна:** «Төлбөр хүлээн авлаа» мэдэгдэл нь
`/profile?course=<programId>` руу заадаг. Профайл нээгдэхэд анхдагч таб нь
«Идэвхтэй сургалт» бөгөөд хүлээгдэж буй бүртгэлийн карт нь «Хүлээгдэж буй»
табд байдаг тул `document.getElementById("course-…")` юу ч олохгүй. Скролл
чимээгүй бүтэлгүйтэж, сурагч зүгээр нэг профайлын дээд хэсгийг хардаг.

**Хэрхэн давтах:**

```
Дансаар бүртгүүлэх → хонхны «Төлбөр хүлээн авлаа» мэдэгдлийг дарах
→ /profile?course=<id> нээгдэнэ
→ хуудас «Идэвхтэй сургалт» табаар нээгдэж, ямар ч карт руу шилжихгүй
```

**Эрсдэл:** бага. Мэдээлэл алдагдахгүй, сурагч гараар таб солиход л картаа
олно. Гэхдээ мэдэгдэл дарсан хүн яг тэр зүйлээ хармааргүй байгаа нь
мэдэгдлийн зорилгыг сулруулна.

**Боломжит засвар:** `?course=` ирэхэд тухайн бүртгэлийн төлвөөр табыг нь
сонгох (`registrations.find(...)?.status !== "active"` бол `pending` таб
руу). Мөн `ProfileClient.tsx:66-67` дахь тайлбар хуучирсан — «for a yearly
programme, opened» гэсэн зан төлөв `f431fec`-ээр устсан.

---

## #7 — Бодлого 1000-аас олон болмогц засварлах боломжгүй болно — ✅ ЗАССАН

**Хаана:** [src/app/api/admin/problems/[id]/route.ts:20](src/app/api/admin/problems/%5Bid%5D/route.ts:20)
(мөн [src/lib/assessment/db.ts:353](src/lib/assessment/db.ts:353) — `listProblems`)

**Юу болж байсан:** Бодлого засах PUT нь тухайн бодлогыг id-гаар нь ШУУД
хайхын оронд `listProblems({ includeInactive: true })`-ээр **бүх** бодлогыг
татаад санах ойд `.find()` хийдэг байв. `listProblems` хуудаслалтгүй байсан
тул PostgREST 1000 мөрөөр таслана. Жагсаалт `created_at`-аар эрэмбэлэгддэг
тул **хамгийн шинэ** бодлогууд эхний 1000-д багтахаа больж, засах гэхэд
«Бодлого олдсонгүй» гэсэн 404 буцдаг байлаа.

**Хэрхэн давтах:**

```
problems хүснэгтэд 1000-аас олон мөр байхад:
POST /api/admin/problems      → 200, шинэ бодлого үүснэ
PUT  /api/admin/problems/<id> → 404 "Бодлого олдсонгүй"
```

**Өмнөх бичилтийн залруулга.** Энэ бүртгэлд анх «Устгах DELETE ижил замаар
явдаг» гэж бичсэн нь **буруу** байсан. DELETE нь `deactivateProblem` →
`updateProblem` → `.eq("id", id)` гэсэн ганц мөрийн UPDATE тул 1000 мөрийн
таслалтад огт хамаарахгүй, 1000+ мөртэй үед ч зөв ажиллаж байсан. Зөвхөн PUT
эвдэрсэн. (Мөн `listProblems`-ийн мөрийн дугаар 322 биш 353 байсан.)

**Эрсдэл нь бодсоноос ноцтой байсан.** Админы 404 нь өөрийгөө зарладаг тул
хамгийн аюулгүй хэсэг нь байжээ. Харин `listProblems`-ийн таслалт нь дараах
гурвыг **чимээгүй** эвддэг байсан:

- [solutions/route.ts:31](src/app/api/assessment/%5Bid%5D/solutions/route.ts:31) —
  сурагч шалгалт өгч байх үеийн маршрут. 1000-аас цааш орсон бодлого
  `problem: null` болж, хүүхэд **асуултгүй хоосон карт** харна. Зураг оруулах
  ч, «Бодож чадсангүй» дарах ч ажиллахгүй (`SolutionUploader.tsx:103,129` нь
  `!step.problem` дээр эрт буцдаг), улмаар «N бодлогын бодолт дутуу байна»
  гэж илгээж ч чадахгүй. Төлбөртэй шалгалт гарц нь мухардана.
- [gradingDetail.ts:28](src/lib/assessment/gradingDetail.ts:28) — багш
  «Бодолт шалгах» дээр хүүхдийн гар бичмэлийг хардаг ч бодлогын эх, зөв
  хариу хоёул алга болно. Оноо тавих талбар нь хэвээр байх тул алдаа
  мэдэгдэхгүй.
- [report.ts:34](src/lib/assessment/report.ts:34) — хүүхэд, эцэг эх
  «Багшийн дүгнэлт» дээр оноог хардаг ч ямар бодлогын оноо болохыг харахгүй.

**Засвар:** хоёр хэсэгтэй.

1. `findProblemById(id)` нэмж, PUT-ыг түүгээр солив. Ганц мөрийн асуулга тул
   санд хэдэн мөр байгаагаас хамаарахгүй. `22P02` (uuid биш id) хамгаалалт нь
   өмнөх зан төлөвийг хадгална — буруу бичсэн id 500 биш, 404 буцна.
2. `listProblems`-ыг `fetchAllRows`-оор хуудаслав. `created_at` дээр нэмж
   `.order("id")` тавьсан нь заавал шаардлагатай: `now()` бол гүйлгээний
   эхлэлийн цаг учир **нэг INSERT-ийн бүх мөр яг ижил `created_at`-тай**
   болно. Давхардсан түлхүүр дээр хуудасны зааг тогтворгүй болж, зарим мөр
   хоёр удаа ирж, зарим нь огт ирэхгүй болдог. AI-аар бодлого багцлан
   үүсгэвэл яг ийм багц үүснэ. Энэ бол `getLastLoginByUser`
   ([db.ts:748](src/lib/db.ts:748)) дээр аль хэдийн тохиолдсон алдаа.

Тест: [tests/api/content/problems-paging.test.ts](tests/api/content/problems-paging.test.ts).
Өөрөө 1001 мөр тарьдаг тул тестийн санд хэдэн мөр байгаагаас хамаарахгүй —
орчны мөрийн тоонд түшсэн тест нь хогоо цэвэрлэсэн өдөр шүдээ алддаг.
Засваргүйгээр хоёул унана: засварлах нь `404 Бодлого олдсонгүй`, жагсаалт нь
`…(1000)` дээр таслагдана.

---

## #8 — 1000 мөрийн таслалт өөр 19 газар үлдсэн (нэг нь production дээр аль хэдийн буруу)

**Хаана:** олон. Хамгийн чухал нь
[src/lib/db.ts:2369](src/lib/db.ts:2369) (`getPageViewCountsByPrefix`),
[src/lib/db.ts:2666](src/lib/db.ts:2666) + [db.ts:2673](src/lib/db.ts:2673)
(`resolveNotificationRecipients`) + [src/lib/push.ts:43](src/lib/push.ts:43),
[src/lib/rollCall.ts:114](src/lib/rollCall.ts:114),
[src/lib/adminDashboard.ts:101](src/lib/adminDashboard.ts:101).

**Юу болж байна:** #7-той яг ижил анги — хуудаслалтгүй `select` хийгээд
үр дүнг нь тоолох / `Map`, `Set` болгох / `.find()` хийх. PostgREST 1000
мөрөөр тасалж, юу ч дуугардаггүй. `fetchAllRows`
([src/lib/fetchAll.ts](src/lib/fetchAll.ts)) энэ зорилгоор аль хэдийн бий.

### Аль хэдийн буруу (production, 2026-09-09-нд хэмжсэн)

`getPageViewCountsByPrefix` — админы сургалтын жагсаалтын «Харсан» багана.
Production дээр `/courses/` угтвартай **2,781** үзэлт байхад код **1,000**-ыг
л уншиж байна, буюу харагдаж буй тоо бодит утгын ~36%. Түүнээс гадна энэ
асуулгад **`.order()` огт байхгүй** тул аль 1000 мөр ирэх нь тогтворгүй —
хуудсаа дахин ачаалахад тоо өөрчлөгдөж болно. (page_views нийт 15,527 мөр.)

### Гадагш алдагддаг тул хамгийн өндөр өртөгтэй

`resolveNotificationRecipients` + `sendPushToUsers`: «бүгд» / «сурагчид» гэж
мэдэгдэл явуулахад хүлээн авагчид 1000-аар тасарна. `recipient_count` нь
яг 1000 гэж бичигдэх тул логоос ч мэдэгдэхгүй, алдаа ч гарахгүй — зүгээр л
зарим сурагч хичээлийнхээ мэдээллийг сонсохгүй. Одоо users 243 мөртэй тул
**хараахан хөндөгдөөгүй**, гэвч хамгийн чимээгүй тул хамгийн аюултай.

### Цаг хугацааны асуудал (одоогийн мөрийн тоо хаалтанд)

`listUsers` (243) · `listAllRegistrations` (98) · `getDashboardStats` ·
`listCertificates` (71 — давхардлыг хардаг `Set` нь хоёр дахь сертификат
олгохоос сэргийлдэг цорын ганц хамгаалалт) · `listSentReminderKeys`
(давтан сануулга явуулахаас сэргийлдэг idempotency түлхүүр) ·
`rollCall` ирц (432) · `adminDashboard` ирц · `placementDb.ts:111`
(69 — энд бас `.order("id")` tiebreaker хэрэгтэй) ·
`programWaitlist.ts:147` (5) · `waitlist.ts:94` · `assessment/db.ts:903, 963, 641`.

### Шалгаад цэвэр (дахин бүү шалга)

`levels`, `yearly_programs`, `courses`, `articles`, `contract_templates`,
`admin_users`, `exam_problems`, `chat_messages`, `sessions`, нэг хичээлийн
`lesson_attendance` унших, `userTimeline`, нэг шалгалтын хүүхдүүд,
`notification_reads`, `recordingViews`, `paymentIntents`,
`resolveNotificationRecipients`-ийн `"course"` салаа.

**Эрсдэл:** page_views-ийнх нь **одоо** буруу тоо харуулж байгаа тул бага-дунд
(мөнгө, эрхэд хамаагүй, зөвхөн статистик). Мэдэгдлийнх нь ирээдүйд өндөр.
Бусад нь хүснэгт 1000 давах хүртэл тайван.

**Боломжит засвар:** тус бүрд `fetchAllRows` + `.order(<цор ганц түлхүүр>)`.
Зарим асуулгад одоо `.order()` огт байхгүй тул эхлээд нэмэх шаардлагатай.
`.in(...)` дуудлагууд 150-аас олон id-тай бол `chunk()` хэрэглэнэ.
Багцлан хийвэл нэг өдрийн ажил; тус тусад нь ч хийж болно.

---

## #9 — Үнэлгээний гурван унших нь хэрэгтэйгээс 100 дахин их өгөгдөл татдаг

**Хаана:** [src/lib/assessment/report.ts:34](src/lib/assessment/report.ts:34),
[src/lib/assessment/gradingDetail.ts:28](src/lib/assessment/gradingDetail.ts:28),
[src/app/api/assessment/[id]/solutions/route.ts:31](src/app/api/assessment/%5Bid%5D/solutions/route.ts:31)

**Юу болж байна:** гурвуулаа нэг шалгалтын ~10 бодлого хэрэгтэй атлаа
`listProblems({ includeInactive: true })`-ээр **бүх сан**-г татаад
`.find()` хийдэг. #7-ийн засвараар зөв болсон (хуудаслана) ч үр ашиггүй
хэвээр. Репод аль хэдийн зөв загвар бий: `listQuizQuestionsByIds`
([assessment/db.ts:1041](src/lib/assessment/db.ts:1041)) нь `.in("id", ids)`
хэрэглэдэг.

`solutions/route.ts` нь **сурагч тал руу харсан** — бодолтын хуудас ачаалах
бүрд, зураг оруулах бүрд ажилладаг. Сан 3000 бодлого болоход хүүхдийн нэг
хуудас ачаалалт 4 дараалсан Supabase дуудлага, хэдэн MB болно (`answer_key`
ч дамжина — `toPublicProblem` браузер руу гарахаас нь өмнө хасдаг тул
задрал биш, зүгээр л дэмий ачаалал).

Мөн `ProblemsPanel` нь бодлого бүрийг KaTeX-ээр рендерддэг, хуудаслалт ч,
виртуалчлал ч байхгүй ([ProblemsPanel.tsx:441](src/components/admin/ProblemsPanel.tsx:441)) —
5000 бодлоготой үед эхний зурагт 5000 KaTeX рендер хийнэ. `ExamEditor` дээр
яг ижил зүйл: [ExamEditor.tsx:272](src/components/admin/ExamEditor.tsx:272)
нь `available.map(...)`-ыг тайрахгүй, мөр бүрд `MathText` рендерддэг, харин
`max-h-[420px]` хайрцагт ердөө ~6 мөр харагдана. Өмнө нь PostgREST-ийн 1000
мөрийн таслалт нь энэ рендерийг санамсаргүйгээр хязгаарлаж байсан — #7-ийн
засвар тэр хязгаарыг авч хаясан тул энэ хоёрыг сан томрохоос өмнө засах нь
чухал болов.

**Эрсдэл:** одоогоор байхгүй (production 30 бодлоготой). Сан ~1500 давахаас
өмнө хийвэл зохино.

**Боломжит засвар:** гурван газарт `listProblemsByIds(ids)` нэмж
`.in("id", ids)`-ээр солих. `ProblemsPanel`-ийн хайлт, шүүлтийг асуулга руу
буулгаж, `select("*")`-ыг жагсаалтад хэрэглэхээ болих.

---

## #10 — Засах талбар агуулаагүй PUT нь байгаа бодлогыг «олдсонгүй» гэнэ

**Хаана:** [src/app/api/admin/problems/[id]/route.ts:34](src/app/api/admin/problems/%5Bid%5D/route.ts:34)
(+ [src/lib/assessment/db.ts:409](src/lib/assessment/db.ts:409) — `updateProblem`)

**Юу болж байна:** Энэ бол **#5-ийн яг ижил ихэр** — тэнд сургалт дээр илэрч
зассан алдаа бодлого дээр үлдсэн байна. `validateProblemInput(data, {partial:true})`
нь зөвхөн `category, topic, bodyLatex, imageUrl, answerKey, active` гэсэн
зургаан түлхүүрийг уншдаг ([validateProblem.ts:19-50](src/lib/assessment/validateProblem.ts:19)).
`level`, `difficulty` хоёр маягтаас санаатай хасагдсан. Тиймээс эдгээрийг л
агуулсан body ирвэл `value = {}` болж, `updateProblem` хоосон patch үүсгэнэ.
PostgREST хоосон PATCH-д `200 []` буцаах тул `.maybeSingle()` нь `null` өгч,
`updateProblem` `undefined` буцаана → route нь **404 «Бодлого олдсонгүй»**
гэж хариулна. Мөр нь хүснэгтэд бүрэн бүтэн сууж байхад.

**Хэрхэн давтах:**

```
PUT /api/admin/problems/<байгаа бодлогын id>
{ "level": 5, "difficulty": 4.5 }
→ 404 "Бодлого олдсонгүй" (мөр өөрчлөгдөөгүй, устаагүй)
{} буюу таних түлхүүргүй дурын body ижил үр дүн өгнө
```

**Эрсдэл:** бага. Одоогийн админ панелээс хүрэхгүй — `save()` нь маягтыг
бүтнээр нь илгээдэг, `restore()` нь `{active:true}` илгээдэг
([ProblemsPanel.tsx:111,149](src/components/admin/ProblemsPanel.tsx:111)) тул
хоёул хоосон биш. Гэвч «олдсонгүй» гэдэг нь дуудагчид байхгүй мөрийг заана —
хэсэгчилсэн хадгалалт нэмэх, өөр клиент бичих үед чимээгүй төөрөгдүүлнэ.

**Тэмдэглэл:** энэ нь #7-ийн засвараас үүдээгүй, өмнөөс байсан. Засвараас
өмнө ч 1000-аас цааш орсон мөр яг тэр 404-ийг эхний шалгуур дээр авдаг
байсан тул гаднаас харагдах зан төлөв өөрчлөгдөөгүй.

**Боломжит засвар:** #5-ийнхтэй ижил — patch хоосон үед `updateProblem` мөрийг
шинэчлэхийг оролдохоо болиод одоо байгаа бодлогыг эргүүлж уншина. Эсвэл route
нь хоосон `value`-г 400 «Өөрчлөх зүйл алга» гэж хариулна. «Өөрчлөх зүйлгүй»
гэдэг нь «ийм бодлого байхгүй» гэсэн үг биш.
